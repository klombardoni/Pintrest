<h1>Listing pins</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Image</th>
      <th>User</th>
      <th>Board</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pins.each do |pin| %>
      <tr>
        <td><%= pin.name %></td>
        <td><%= pin.image %></td>
        <td><%= pin.user_id %></td>
        <td><%= pin.board_id %></td>
        <td><%= link_to 'Show', pin %></td>
        <td><%= link_to 'Edit', edit_pin_path(pin) %></td>
        <td><%= link_to 'Destroy', pin, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Delete All Pins', Pin, method: :delete, data: { confirm: ' Are You Sure'} %>

<%= link_to 'New Pin', new_pin_path %>

<%= link_to 'Delete All Boards', Board, method: :delete, data: { confirm: ' Are You Sure'} %>

<%= link_to 'New board', new_board_path %>

<%= link_to 'Delete All Users', Users, method: :delete, data: { confirm: ' Are You Sure'} %>

<%= link_to 'New User', new_user_path %>